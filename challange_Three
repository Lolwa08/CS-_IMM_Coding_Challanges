def romanToInt(s):
        roman = 'IVXLCDM'
        num = [1,5,10,50,100,500,1000]
        string = []
        
        for i in s:
            index = roman.find(i)
            string.append(num[index])
        return sum(string)
